<?php
//print_r($_GET);
/*$q = $_GET['q'];

/*foreach ($arr_universidades as $value) {
	echo $value;
};

foreach ($arr_universidades as $key=>$value) {

			$uni = $value[0];
			$id = $value[1];
			
			if (strpos(strtolower($uni), $q) !== false) {
				echo "$uni|$id\n";
			}

		}

*/
		$arr_universidades = array(array("label"=>'UBA',"value"=>1),array("label"=>'UTN',"value"=>2),
			array("label"=>'UADE',"value"=>3));

		echo json_encode($arr_universidades)
?>